# Local Claude Code settings — machine-specific, not for sharing
.claude/settings.local.json

# Worker runtime secrets — never commit
.env

# Kubernetes secrets — never commit
k8s/secret.yaml

# Claude session state — ephemeral, machine-specific
.claude/session-env/
